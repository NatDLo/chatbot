services:
  - type: web
    name: chatbot
    env: python
    pythonVersion: 3.12.13
    buildCommand: pip install -r requirements.txt
    startCommand: cd util && uvicorn main:app --host 0.0.0.0 --port $PORT
