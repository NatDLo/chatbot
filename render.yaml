services:
  - type: web
    name: chatbot
    env: python
    pythonVersion: 3.12.13   # <- aquí defines la versión exacta
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
